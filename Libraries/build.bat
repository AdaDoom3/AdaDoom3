.... TODO
* overwrite all dlls with new versions if the compiler exists to create for the desired bit size library executable
* the dll should all be in the source directory's root (e.g. for sdl it is Libraries/SDL/sdl32.dll)
